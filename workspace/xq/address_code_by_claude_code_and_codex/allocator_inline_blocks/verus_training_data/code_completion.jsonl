{"id": "code_completion.alloc_fast.heap_malloc_zero_ex.branch", "task": "code_completion", "language": "verus", "prompt": "#[inline]\npub fn heap_malloc_zero_ex(heap: HeapPtr, size: usize, zero: bool, huge_alignment: usize, Tracked(local): Tracked<&mut Local>)\n    -> (t: (*mut u8, Tracked<PointsToRaw>, Tracked<MimDealloc>))\n    requires\n        old(local).wf(),\n        heap.wf(),\n        heap.is_in(*old(local)),\n    ensures\n        local.wf(),\n        ({\n            let (ptr, points_to_raw, dealloc) = t;\n            points_to_raw@.is_range(ptr as int, size as int)\n              && points_to_raw@.provenance() == ptr@.provenance\n              && ptr == dealloc@.ptr()\n              && dealloc@.inst() == local.instance\n              && dealloc@.size() == size\n        }),\n        common_preserves(*old(local), *local),\n{\n    if likely(size <= SMALL_SIZE_MAX) {\n", "completion": "        heap_malloc_small_zero(heap, size, zero, Tracked(&mut *local))\n    } else {\n        malloc_generic(heap, size, zero, huge_alignment, Tracked(&mut *local))\n    }\n}", "source": {"file": "allocator_inline_blocks/alloc_fast.rs", "item": "heap_malloc_zero_ex"}}
{"id": "code_completion.alloc_generic.malloc_generic.body", "task": "code_completion", "language": "verus", "prompt": "pub fn malloc_generic(\n    heap: HeapPtr,\n    size: usize,\n    zero: bool,\n    huge_alignment: usize,\n    Tracked(local): Tracked<&mut Local>,\n) -> (t: (*mut u8, Tracked<PointsToRaw>, Tracked<MimDealloc>))\n    requires\n        old(local).wf(),\n        heap.wf(),\n        heap.is_in(*old(local)),\n    ensures\n        local.wf(),\n        ({\n            let (ptr, points_to_raw, dealloc) = t;\n\n            points_to_raw@.is_range(ptr as int, size as int)\n              && points_to_raw@.provenance() == ptr@.provenance\n              && ptr == dealloc@.ptr()\n              && dealloc@.inst() == local.instance\n              && dealloc@.size() == size\n        }),\n        common_preserves(*old(local), *local),\n\n{\n    // TODO heap initialization\n\n    // TODO deferred free?\n", "completion": "    heap_delayed_free_partial(heap, Tracked(&mut *local));\n\n    let page = crate::page::find_page(heap, size, huge_alignment, Tracked(&mut *local));\n    if unlikely(page.is_null()) {\n        todo();\n    }\n\n    if unlikely(zero && page.get_block_size(Tracked(&*local)) == 0) {\n        todo(); loop{}\n    } else {\n        crate::alloc_fast::page_malloc(heap, page, size, zero, Tracked(&mut *local))\n    }\n}", "source": {"file": "allocator_inline_blocks/alloc_generic.rs", "item": "malloc_generic"}}
{"id": "code_completion.layout.is_page_ptr_opt.match", "task": "code_completion", "language": "verus", "prompt": "pub open spec fn is_page_ptr_opt(pptr: *mut Page, opt_page_id: Option<PageId>) -> bool {\n    match opt_page_id {\n", "completion": "        Some(page_id) => is_page_ptr(pptr, page_id) && pptr.addr() != 0,\n        None => pptr.addr() == 0,\n    }\n}", "source": {"file": "allocator_inline_blocks/layout.rs", "item": "is_page_ptr_opt"}}
{"id": "code_completion.dealloc_token.into_user.body", "task": "code_completion", "language": "verus", "prompt": "pub(crate) proof fn into_user(tracked self, tracked points_to_raw: PointsToRaw, sz: int)\n    -> (tracked res: (MimDealloc, PointsToRaw))\n\n    requires\n        self.wf(),\n        points_to_raw.is_range(self.ptr as int, self.block_id().block_size as int),\n        points_to_raw.provenance() == self.ptr@.provenance,\n        0 <= sz <= self.block_id().block_size,\n    ensures ({\n        let (md, points_to_raw) = res;\n        points_to_raw.is_range(self.ptr as int, sz)\n        && points_to_raw.provenance() == self.ptr@.provenance\n        && md.size() == sz\n        && md.block_id() == self.block_id()\n        && md.ptr() == self.ptr\n        && md.inst() == self.mim_instance\n    })\n{\n", "completion": "    let tracked (x, y) = points_to_raw.split(set_int_range(self.ptr as int, self.ptr as int + sz));\n    let tracked md = MimDealloc { padding: y, _size: sz, inner: self };\n    (md, x)\n}", "source": {"file": "allocator_inline_blocks/dealloc_token.rs", "item": "MimDeallocInner::into_user"}}
{"id": "code_completion.page.find_page.branch", "task": "code_completion", "language": "verus", "prompt": "pub fn find_page(heap_ptr: HeapPtr, size: usize, huge_alignment: usize, Tracked(local): Tracked<&mut Local>) -> (page: PagePtr)\n    requires\n        old(local).wf(),\n        heap_ptr.wf(),\n        heap_ptr.is_in(*old(local)),\n    ensures\n        local.wf(),\n        common_preserves(*old(local), *local),\n        page.page_ptr.addr() != 0 ==> page.wf() && page.is_in(*local)\n            && page.is_used_and_primary(*local),\n        page.page_ptr.addr() != 0 ==> \n            local.pages.index(page.page_id@).inner.value().xblock_size >= size,\n{\n    proof { const_facts(); }\n\n    let req_size = size;\n    if unlikely(req_size > MEDIUM_OBJ_SIZE_MAX as usize || huge_alignment > 0) {\n", "completion": "        if unlikely(req_size > MAX_ALLOC_SIZE) {\n            return PagePtr::null();\n        } else {\n            todo(); loop { }\n        }\n    } else {\n        return find_free_page(heap_ptr, size, Tracked(&mut *local));\n    }\n}", "source": {"file": "allocator_inline_blocks/page.rs", "item": "find_page"}}
